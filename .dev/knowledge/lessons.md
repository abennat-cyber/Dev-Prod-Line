# Lessons

Append-only record of lessons captured at session end by
`.dev/hooks/session_memory.py`. Recurring lessons must be promoted into
`.dev/standards/` or `.dev/guardrails/` via
`.dev/procedures/promote-lessons.md`, then pruned here.
